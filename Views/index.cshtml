<h1>Contacts</h1>
<ul>

@if (@Model.Count == 0)
  {
    <h2>You Contacts are empty!</h2>
  }
else
{
  @foreach (var contact in Model)
  {
    <li><a href="/view_all_contacts/@contact.GetId()">@contact.GetName()</a></li>
  }
}

</ul>
  <a href="/contact/new_contact">add a new contact</a>
